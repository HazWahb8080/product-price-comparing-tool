import { atom } from "recoil";

export const AmazonResultsState = atom({
  key: "AmazonResultsState",
  default: [],
});
export const BestBuyResultsState = atom({
  key: "BestBuyResultsState",
  default: [],
});
export const WalmartResultsState = atom({
  key: "WalmartResultsState",
  default: [],
});
