import { atom } from "recoil";

export const AmazonResultsState = atom({
  key: "AmazonResultsState",
  default: [],
});

export const EbayResultsState = atom({
  key: "EbayResultsState",
  default: [],
});
